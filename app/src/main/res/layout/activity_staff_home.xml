<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/orange"
    tools:context=".StaffHome">

    <!-- 🗺️ Top Section: Map -->
    <com.google.android.gms.maps.MapView
        android:id="@+id/mapView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5" />

    <!-- ✅ Active Switch -->
    <com.google.android.material.materialswitch.MaterialSwitch
        android:id="@+id/activeSwitchStaff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:textColor="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- 🏠 Middle Section: Monitoring Contacts -->
    <LinearLayout
        android:id="@+id/contactsContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/mapView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/sosSlider">

        <!-- 🔽 Placeholder Contact Card 1 -->
        <LinearLayout
            android:id="@+id/contact_placeholderStaff_1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:background="@drawable/contact_placeholder_background"
            android:orientation="vertical"
            android:padding="16dp"
            android:elevation="4dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/contact_textStaff_1"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Name: Julio Caesar"
                    android:textSize="18sp"
                    android:textColor="@color/light_orange"
                    android:textStyle="bold"
                    android:paddingStart="16dp" />

                <ImageButton
                    android:id="@+id/open_button"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:src="@drawable/ic_arrow_right"
                    android:background="?android:attr/selectableItemBackgroundBorderless" />
            </LinearLayout>

            <!-- 🌟 Expanded View (Initially Hidden) -->
            <LinearLayout
                android:id="@+id/contact_placeholderStaffOpen_1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="8dp"
                android:visibility="gone"
                android:animateLayoutChanges="true"> <!-- Smooth transition -->

                <TextView
                    android:id="@+id/contact_detailsStaff_1"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Details: Available for Monitoring"
                    android:textSize="16sp"
                    android:textColor="@color/white" />
            </LinearLayout>
        </LinearLayout>

        <!-- 🔽 Placeholder Contact Card 2 -->
        <LinearLayout
            android:id="@+id/contact_placeholderStaff_2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:background="@drawable/contact_placeholder_background"
            android:orientation="vertical"
            android:padding="16dp"
            android:elevation="4dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/contact_textStaff_2"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Name: Marcus Aurelius"
                    android:textSize="18sp"
                    android:textColor="@color/light_orange"
                    android:textStyle="bold"
                    android:paddingStart="16dp" />

                <ImageButton
                    android:id="@+id/open_button_2"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:src="@drawable/ic_arrow_right"
                    android:background="?android:attr/selectableItemBackgroundBorderless" />
            </LinearLayout>

            <!-- 🌟 Expanded View (Initially Hidden) -->
            <LinearLayout
                android:id="@+id/contact_placeholderStaffOpen_2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingTop="8dp"
                android:visibility="gone"
                android:animateLayoutChanges="true">

                <TextView
                    android:id="@+id/contact_detailsStaff_2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Details: Assigned to Night Patrol"
                    android:textSize="16sp"
                    android:textColor="@color/white" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- 🚨 Bottom Section: SOS Slider -->
    <FrameLayout
        android:id="@+id/sosSlider"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@drawable/slider_background"
        android:padding="8dp"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/sliderInstruction"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Slide to Alertify"
            android:textSize="20sp"
            android:fontFamily="@font/bangers_regular"
            android:textColor="@color/primary_yellow"
            android:gravity="center"
            android:layout_gravity="center" />

        <FrameLayout
            android:id="@+id/sliderButton"
            android:layout_width="65dp"
            android:layout_height="65dp"
            android:background="@drawable/circle_background"
            android:elevation="8dp"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="0dp">

            <ImageView
                android:id="@+id/sliderIcon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_arrow_right"
                android:layout_gravity="center" />
        </FrameLayout>
    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
